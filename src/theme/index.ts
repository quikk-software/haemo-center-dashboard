import { Roboto } from "next/font/google";
import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: { main: "#d7263d" },
    secondary: { main: "#4E52B5" },
  },
});

export const LIGHT_HEX_OPACITY = "14";
